# Optional: required to use format="jxl" in SaveImagePlus
pillow-jxl-plugin>=1.3.0 ; python_version >= "3.8"

# Required for ZSimple Anthropic Agent
anthropic>=0.40.0

# Required for ZSimple OpenAI Agent
openai>=1.0.0

# Required for SaveVideoPlus (mp4/webm encoding; gif uses Pillow)
imageio-ffmpeg>=0.5.0